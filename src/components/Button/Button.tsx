import styled from "styled-components";

export const Button = styled.button`
  margin: 0;
  padding: 0;
  font-family: var(--font-monospace);
  font-size: 1rem;
  border: none;
  border-radius: 0;
  line-height: 1.2rem;
`;
